package scala2021.skarasik.task04

import scala.annotation.tailrec

object Task04 {
  def main(args: Array[String]): Unit = {
    // ToDo: solve
  }

//    @tailrec
//    def canGiveChange(coins: Seq[Int], change: Int): Boolean = {
//      if (coins.contains(change)) {
//        true
//      } else {

//      }
//  }
}
